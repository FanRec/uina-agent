# Uina 重构规划

> 基于 `eb56fd1..15aedec`（26 提交 / 61 文件 / +5852 −838）的架构审查结论。
> 审查视角：第一性原则 · 对抗式审查 · 奥卡姆剃刀 · 高内聚低耦合 · 精简内核 · 反自我欺骗。
> 配套进度文件：`.refactor-progress.json`
> 基线：HEAD = `15aedec`，490 测试全绿，`tsc --noEmit` 干净。
> 生成时间：2026-09-13 21:43 (+08:00)

---

## 0. 文档定位

这是一份**可执行的**重构规划，不是审查报告的复述。它的每一行都对应一个可验证的动作，且刻意区分三件事：

- **已核实的事实**（有文件:行号、有实测输出）
- **推断**（有证据但非编译期证明，标注出来）
- **不做的事**（奥卡姆：明确拒绝，避免范围蔓延）

进度以 `.refactor-progress.json` 为准，本文档描述"为什么"和"怎么做"。

---

## 1. 范围与基线

| 项 | 值 |
|---|---|
| 审查范围 | `eb56fd1..15aedec` |
| 提交数 | 26（全部落在 2026-09-13 单日） |
| 文件数 | 61 |
| 增删 | +5852 / −838 |
| HEAD | `15aedec` fix(ui): count real output tokens per model call, not per usage_update |
| 测试 | 490 passed / 34 files |
| 覆盖率 | Statements 72.36% · Branches 66.3% · Functions 70.16% · Lines 74.7% |
| 类型检查 | `strict` + `noUnusedLocals` + `noUnusedParameters`，干净 |
| 证据环境 | `E:/Uina/.audit/Uina-cov`（`git archive HEAD` 快照 + `@vitest/coverage-v8@4.1.11`，与主仓库隔离） |

---

## 2. 判断基准（先定尺子，再动手）

Uina 是**单机、无 CI、无覆盖率工具、中文、DeepSeek 后端**的个人 harness。尺子必须适配这个事实，而不是适配教科书：

| 尺子 | 对 Uina 的含义 | 该避免的反例 |
|---|---|---|
| **精简内核** | Core 拥有领域真相（会话、回溯、压缩、执行循环）；外壳（UI/CLI/Extension）只消费 | 把 hover 编码、分支投影塞进内核 |
| **高内聚** | 一份真相只有一个所有者，且**读写同源** | id 编码与解码相隔 150 行、格式靠人脑同步 |
| **奥卡姆** | 不为假想的第二实现写兼容分支 | `thinking-` / `tool-` 死分支 |
| **可用性 > 数值漂亮** | 用零成本编译选项 + 纯函数单测 + 真机验收 | 引入 eslint / coverage 门禁 |
| **反自我欺骗** | 测试断言**行为**；注释与提示不得承诺不存在的行为；不留"只被测试用"的后门 | `getTpsSamples()`、"←→ 翻页详情" |

> **战略锚点**：Uina 的护城河是**会话 / 回溯 / 压缩内核对不对**，不是 UI 花不花。因此重构预算优先投给 `session/` 与 `agent/` 的契约清晰度；UI 侧只做"收口 + 去死码"。

---

## 3. 实测证据（全部已核实，非推断）

| 证据 | 来源 |
|---|---|
| 开 `allowUnreachableCode:false` 后全仓**仅 2 处** TS7027 | 隔离环境 `tsc -p tsconfig.probe.json` 实测 |
| `noFallthroughCasesInSwitch` / `noImplicitOverride` = **0 错误** | 同上 |
| `branch-inspector.ts:36` 不可达（`:35` 无条件 return） | 实读源码 |
| `cli/app.ts:161` 不可达（`process.exit(0)` 之后 return） | 同上；**上一轮审查漏报** |
| `:78-81` 详情翻页被 `:59` 无条件 return 遮蔽 | 实读源码 |
| `:170-172` 提示 `←→ 翻页详情 X/Y` 与行为不符 | 实读源码 |
| `navigation.ts:108-109` 与 `:118-119` 逐字重复 | 实读源码 |
| `navigation.ts:120` 重算全部分支 + `!` 断言 | 实读源码 |
| `navigation.ts:105-122` 风格与全文件不符（单空格 / 行内多语句） | 实读源码 |
| preview 两份实现（`navigation.ts:12` / `branch-inspector.ts:34`） | 实读源码 |
| `tpsSamples` **驱动渲染**（`:328` sparkline / `:345` peak） | 实读源码 |
| `getTpsSamples` **src 内 0 消费方**，仅测试用 | 全仓 grep |
| `mouse-selection.ts:141 getHoverTargetId` **0 消费方** | 全仓 grep |
| `thinking-` / `tool-` 前缀**无生产者** | 全仓 grep + 生产者实读（`ui-host.ts:1264-1441`） |
| 测试 `accessFor()` 不实现 `listBranches`/`readBranch` → 交互语义 0 覆盖 | 实读测试 |

### 3.1 生产者—消费者对账（hover 协议）

```
ui-host.ts:1277  id: `thinking:${loc.item.uid}`            ← 冒号
ui-host.ts:1301  id: `tool:${loc.callId}:${absLine}`       ← 冒号
ui-host.ts:1325  id: `compaction:${loc.index}:${absLine}`  ← 冒号
ui-host.ts:1361  id: `scrollbar-row-${r}`                  ← 连字符
ui-host.ts:1398  id: `rail-tick-${turn.uid}`               ← 连字符
```

消费端 `ui-host.ts:1536-1606` 里，`thinking-`（`:1538-1539`）与 `tool-`（`:1549-1551`）**没有任何生产者**。

---

## 4. 分期规划

### P0 — 消灭确凿的谎言与死码（零风险，先做）

| # | 改动 | 依据 | 验收 |
|---|---|---|---|
| P0.1 | 删 `branch-inspector.ts:36` 死 `return []` | TS7027 实测 | tsc 干净 |
| P0.2 | 删 `cli/app.ts:161` 死 `return` | TS7027 实测 | tsc 干净 + 真机退出码不变 |
| P0.3 | 删 `thinking-` / `tool-` 死分支 | 无生产者 | 新 decode 单测对旧逻辑必红 |
| P0.4 | 修 `:59-66`：加 `focusTarget === "detail"` 守卫使 `:78-81` 生效（推荐），或删 `:170-172` 提示 | 二者必须一致 | 交互单测：detail 焦点下 ←/→ 移动 offset |
| P0.5 | 删 `getTpsSamples()`，测试改断言渲染输出 | `tpsSamples` 驱动渲染，行为可观测 | 变异：清空 `tpsSamples` 则该测试必红 |
| P0.6 | 删 `mouse-selection.ts:141 getHoverTargetId()` | 0 消费方 | tsc + 测试 |

**P0.4 的独立判断**：倾向**方案 A（修复翻页）**而非删提示。理由——提示文案已经承诺了这个能力，`detailScrollOffset` 的 clamp 逻辑（`:158`）也已就绪，删除等于**用砍功能来消除不一致**；修复才是兑现承诺。

### P4 — 防复发机制（先把编译器武装起来，成本极低）

| # | 改动 | 实测依据 | 风险 |
|---|---|---|---|
| P4.1 | `allowUnreachableCode: false` | 实测仅 2 处错误，P0.1/P0.2 清掉后即绿 | 零 |
| P4.2 | `noFallthroughCasesInSwitch: true` | 实测 0 错误 | 零 |
| P4.3 | `noImplicitOverride: true` | 实测 0 错误 | 零 |
| P4.4 | `"test": "vitest run"` | 无 CI，手动跑是最低成本门禁 | 零 |

> **这是本次审查最有价值的产出**：它把"不可达代码"从"靠人眼发现"变成"**编译器强制**"。26 个提交里我只用眼找到 1 处，编译器一次就找出 2 处——**工具比人可靠**。

### P1 — 收口真相（高内聚，改动小、收益长期）

| # | 改动 | 依据 | 验收 |
|---|---|---|---|
| P1.1 | 新建 `src/ui/core/hover-target.ts`：`encodeHoverTarget()` / `decodeHoverTarget()` 纯函数 + 判别联合 | 生产/消费相隔 150 行，格式靠人脑同步 | 纯函数单测（不涉时序，可真红真绿）+ 往返一致 |
| P1.2 | `ui-host.ts` 生产端（1264-1441）与消费端（1535-1606）接入 | 同上 | 同上 + `handleTerminalInput` 减约 70 行 |
| P1.3 | `navigation.ts` 抽 `branchNodeRange()`，两处复用；缩进统一为 tab | 逐字重复 + 风格不一致 | 单测：两函数返回一致 |
| P1.4 | `navigation.ts:120` 用已找到的 `rewind` 直接构造，删 `!` 与重算 | 冗余 + 掩盖 | 变异：删存在性检查则「未知分支」用例红 |

> **P1.1 的战略价值**：把"鼠标热区协议"从**隐式字符串**变成**显式类型**。此后新增热区只需改一处，编译器通过 `switch` + `never` 强制穷尽。它是纯逻辑、**可单测**——正面对冲"滚动/鼠标时序测不出"的痛点。

### P2 — 补齐内核能力缺口（职责归属）

| # | 改动 | 依据 | 验收 |
|---|---|---|---|
| P2.1 | `navigation.ts` 新增 `listBranchNodes()`：分支的节点视图 | UI 无法复用领域投影 | 单测：视图内容 + 只读语义 |
| P2.2 | `branch-inspector.nodes()` 改为**纯委派**，删自行投影与 `:35` 错误兜底 | UI 不应承载业务规则 | 交互语义单测（补 mock） |
| P2.3 | `SessionAccess` 的 `listBranches?` / `readBranch?` 由**可选改为必需** | 生产实现总是提供；唯一不提供者是测试 mock | tsc 强制所有实现者 |
| P2.4 | 分支 preview 模板下沉至 `navigation.ts`（随 P2.1） | 两份实现会漂移 | 单测：preview 文案 |

> **P2.3 的理由（奥卡姆的反面用法）**：可选性看似"灵活"，实际代价是**每个消费者都要写一份语义可疑的降级**。契约应当表达"生产环境的事实"——生产者永远提供，就不该标可选。这也顺带删掉 `:35` 的错误兜底。

### P3 — 结构性债务（记录，本次不动手）

| # | 项 | 判断 |
|---|---|---|
| P3.1 | `Subject`（`loop.ts` 1184 非空行 / 总 1266 行 / 84 方法）拆分 | **暂不拆**。avgCov 87%、仅 1 个 CRAP>30；拆分需共享 `history`/`store`/`abort`，硬拆会引入跨对象隐式同步（违反高内聚） |
| P3.2 | `ui-host.ts`（1808 非空行 / 总 2002 行）拆分 | **暂不拆**。但 P1.1/P1.2 已自然削减其最复杂函数 |

> **为什么先不拆 `Subject`**：第一性原则问的是"**当前**它有没有造成真实缺陷"。答：没有。**为假想的未来拆分 = 制造新的复杂度**。记录为债务，等它下一次真实出问题时再拆——那时才有真实边界。

---

## 5. 不做清单（奥卡姆剃刀）

1. **不拆 `Subject` / `ui-host.ts`** —— 无真实缺陷驱动。
2. **不引入 eslint / coverage 门禁 / prettier** —— 个人项目、无 CI，收益 < 配置与维护成本。
3. **不重写鼠标/滚动时序** —— 违背"滚动时序单测测不出"，且未确证有 bug。
4. **不追求 CRAP 数值** —— `cc` 口径含 `??` / `?:`，数值不可跨项目比较，只用作相对排序。
5. **不动 `compaction.ts`** —— 94% 覆盖、0 个 CRAP>30，本次最健康模块（"能不碰就不碰"的示范）。
6. **不改 build 流程** —— 用户明确约束。

---

## 6. 长期战略

### 6.1 内核边界应这样收敛

```
Core（真相）
  agent/loop · session/{types,jsonl-store,navigation,recovery} · extensions/runner · host
      ↑ 只暴露契约（SessionAccess 等），且契约 = 生产事实，非可选

外壳（表达）
  ui/** · cli/** · extensions/**
      ↑ 只消费契约；不造领域投影；不持有私有跨层协议
```

### 6.2 三条可长期执行的纪律

1. **一份真相一个所有者，且读写同源** —— `navigation.ts` 的重复、hover 编码的分裂，都源于违反此条。
2. **契约只描述生产环境的事实** —— `?` 可选只在"真有多种合法实现"时才用；为测试放宽契约 = 把测试的便利转嫁给每个消费者。
3. **能单测的收口到纯函数，不能单测的留真机验收** —— 不假装测过。这正是 P1.1 把鼠标协议纯函数化的战略意义。

### 6.3 战略押注

Uina 与 Pi 的差距不在功能多少，而在**内核能否被信任**。P0/P1/P2 全部围绕"让内核只讲真话"——死码不留、契约不虚、投影不漂、后门不设。

---

## 7. 执行顺序与验收门

```
P0（死码 + 谎言）
  └─ G1: tsc 干净 · 490 全绿 · 每条新测试对旧逻辑必红（变异 + 回读确认）
      ↓
P4.1–P4.3（编译选项）
  └─ G2: tsc 干净（若报 TS7027 = P0 没清完）
      ↓
P1（收口）
  └─ G3: hover-target 纯函数单测 · handleTerminalInput 行数下降 · 真机验收鼠标全流程
      ↓
P2（契约）
  └─ G4: 分支交互语义单测 · 真机 Alt+H 验收 · tsc 强制实现者
      ↓
P3（记录不报）
  └─ G5: 债务清单更新，无代码改动
```

### 每条改动的硬性要求

- 变异验证**必须回读确认变异真的落盘**；还原**一律用 `.bak-*` 备份文件**，禁用 `git checkout`；
- 字符串替换走 **Node 脚本文件 + 唯一性命中校验**，不嵌命令行；
- 涉及鼠标/滚动的 P0.3 / P0.4 / P1.1 / P1.2 **真机验收**，不靠单测假装；
- 每步**独立提交**，可单独回滚；
- 改完源码由**用户重启 Uina** 验收，不代为重启。

---

## 8. 规模估计

| 阶段 | 文件数 | 净行数 | 风险 |
|---|---|---|---|
| P0 | 4（2 src + 1 test + 1 配置） | −25 ~ +40 | 极低 |
| P4 | 1（tsconfig） | +3 | 零 |
| P1 | 3（含 1 新文件） | +120 / −95 | 低（纯函数） |
| P2 | 3 | +90 / −60 | 中（需真机） |
| **合计** | **~10** | **约 +130 净增，删除约 180 行冗余/死码** | — |

---

## 9. 风险与诚实边界

**推断项（非编译期证明）**：

- P0.3 的"死分支"判断基于"全仓 grep 无生产者"，是**强证据但不是编译期证明**。删除后需真机 hover 复核。
- P0.6 的"0 消费方"同为 grep 结论，未覆盖动态属性访问（如 `obj["getHoverTargetId"]`）；Uina 内未发现此类用法。

**未做的**：

- 未通读 `loop.ts`（非空 1184 / 总 1266）、`ui-host.ts`（非空 1808 / 总 2002）、`recovery.ts`（非空 705 / 总 755）全文；P3 的"不拆"结论基于覆盖率 + CRAP + 结构抽样。
- 未评估 `tests/` 的整体质量（只精读了 `branch-inspector.test.ts`）。

**依赖用户确认**：

- P2.3 改必需方法会破坏仓库外任何自定义 `SessionAccess` 实现（Uina 为单仓，但需确认）。
- `cli/app.ts:161` 的引入提交需 `git blame` 确认（不影响修复本身）。

**方法学局限**：

- **CRAP 对 subprocess 路径系统性高估**：`src/cli/app.ts` 显示 `cov=0%`、`runApp` CRAP=6972，但 `tests/cli-session.test.ts` 有 4 处 `spawn` 真实执行 CLI，`scripts/verify-built-cli.mts` 亦覆盖。**其 CRAP 数值不可采信**；结构性观察（巨型组合根）仍成立。

---

## 10. 附录：审查数据

### 10.1 改动文件覆盖与 CRAP（`bad` = CRAP>30 的函数数）

```
 fn  bad  maxCrap avgCov  file
 64    7   6972.0     0%  src\cli\app.ts              ← 伪影（见 9. 方法学局限）
169    4   1498.5    50%  src\ui\ui-host.ts
 59    2    633.9    73%  src\ui\components\editor\input-line.ts
 26    4    371.8    19%  src\extensions\builtin.ts
 20    2    138.9    83%  src\ui\components\overlays\branch-inspector.ts
 60    2     94.7    37%  src\host\host.ts
 21    1     80.3    84%  src\ui\components\transcript\tool-view.ts
 85    1     76.1    87%  src\agent\loop.ts
  7    1     73.3    53%  src\ui\core\keys.ts
 45    2     53.5    88%  src\session\recovery.ts
 22    1     42.0    39%  src\session\navigation.ts
 22    1     42.0    77%  src\ui\core\utils.ts
 30    1     35.8    70%  src\ui\tui.ts
  6    2     30.1    50%  src\extensions\session-tools\index.ts
 11    0     30.0    90%  src\agent\context.ts
 97    0     28.3    79%  src\ui\components\transcript\transcript.ts
148    0     27.5    75%  src\extensions\runner.ts
 13    0     20.2    37%  src\ui\components\transcript\stream-markdown.ts
 28    0     15.0    90%  src\ui\components\widgets\activity-line.ts
 34    0     13.7    78%  src\session\jsonl-store.ts
 28    0     11.6    94%  src\agent\compaction.ts     ← 最佳
  6    0      9.4    93%  src\agent\stream-collector.ts
```

### 10.2 最成功与最需关注

- **最成功**：`compaction.ts`（+499 行，本次最大改动之一）→ **94% 覆盖、0 个 CRAP>30**。纯函数化 + 针对性测试的直接体现。
- **最需关注**：`ui-host.ts:1501 handleTerminalInput` **cc=127 / cov=56% / 473 行** —— `eb56fd1` 既有，但本次在其内新增约 20 个 hover 分派分支，**复杂度被本次加剧**。
- **本次新增的低覆盖点**：`branch-inspector.ts:218 formatNodeDetails`（cov 25%）、`loop.ts:1101 compactProjectionForRewind`（cov 28%）、`navigation.ts:115 readSessionBranch`（cov 0%）。

### 10.3 本次 26 提交的正面结论（有数据支撑）

- 3291 个新增行中：**0 空 catch、0 `as any`、0 `as unknown as`、0 `@ts-ignore`、0 eslint-disable** —— 类型纪律克制。
- 既有"静默 catch"经 `git blame` 确认**全部早于 `eb56fd1`**（`e84f6bb2` 09-04、`decf5522` 09-03），非本次引入，且场景正当。
- 事件总线隔离（`loop.ts:167`）走 `reportError`，不静默；`finally` 资源收口一致。
